#include <stdio.h>

int main() {
    int n; 
    printf("%ld\t%ld\t%ld\n", sizeof +1, sizeof(double) - 1, sizeof(n + 2));                                                              
    printf("%ld\t%ld\t%ld\n", sizeof +1, sizeof(double) - 1, sizeof(n + 2)); 
    printf("%ld\t%ld\t%ld\n", sizeof - 1, sizeof((double)-1), sizeof (n + 2.0)  ); 
}
